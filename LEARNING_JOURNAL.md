# AI Agent 学习档案

> 学习目标：从 AI Agent 基础开始，完成一个可以演示、测试和部署的企业级项目，并将它作为求职作品。

## 一、个人学习情况

- 开始日期：2026-08-18
- 每日可投入时间：1～2 小时
- 当前基础：具备一定 Python 基础，尚未接触 Agent 开发
- 学习方式：先用大白话理解概念，再结合企业场景写代码验证
- 项目背景：杭州海询科技有限公司的海洋设备研发、生产、销售和技术服务场景
- 最终项目：海洋设备智能售前与技术服务 Agent

## 二、最终项目目标

系统面向客户、销售人员和技术服务人员，计划逐步实现：

- 理解客户的海洋观测需求
- 查询海洋设备型号及技术参数
- 根据水深、观测参数和续航等条件辅助选型
- 检索产品说明书、技术资料和售后知识
- 辅助诊断设备故障
- 创建和查询技术服务工单
- 将销售、选型、售后等任务分配给专业 Agent
- 对报价、承诺和高风险操作进行人工审批
- 保存会话与业务状态
- 通过 FastAPI 对外提供服务
- 使用测试和评估检查 Agent 的可靠性
- 使用 Docker 完成部署

## 三、10 周学习计划

| 周次 | 学习主题 | 每周成果 |
|---|---|---|
| 第1周 | Python、异步编程、API、Pydantic 复习 | 异步调用 API，并完成结构化数据校验 |
| 第2周 | 大模型 API、Prompt、结构化输出 | 海洋设备需求分类器 |
| 第3周 | OpenAI Agents SDK、单 Agent、Tools | 能调用产品查询工具的选型 Agent |
| 第4周 | Session、状态、错误处理、Tracing | 支持多轮对话和运行追踪 |
| 第5周 | Handoff、Agent as Tool、Guardrail、人工审批 | 销售、选型、售后多 Agent 原型 |
| 第6周 | RAG、Embedding、文档切分和检索 | 产品与技术资料知识库 |
| 第7周 | FastAPI、数据库、项目分层 | 可以通过 HTTP 调用的 Agent 服务 |
| 第8周 | 自动化测试、Evals、安全和幂等性 | 至少30条评估案例和评估报告 |
| 第9周 | Docker、部署、日志和项目包装 | 可一键启动的求职项目 |
| 第10周 | LangGraph、项目复盘和面试准备 | LangGraph 核心流程及面试材料 |

## 四、每日学习节奏

每天按照下面的节奏进行，可以根据当天时间压缩或延长：

1. 复习昨天内容：10分钟
2. 学习一个新概念：20～30分钟
3. 编写和运行代码：40～70分钟
4. 总结并提交 Git：10分钟

每次学习结束时，至少留下以下一种成果：

- 一段可以运行的代码
- 一条修复后的错误记录
- 一组测试案例
- 一段用自己语言写的概念总结
- 一次 Git 提交

## 四-A、主要教材与选读路线

主要中文教材：Datawhale《从零开始构建智能体》（Hello-Agents）。

使用原则：把它作为原理教材和代码练习来源，不机械地按16章全部顺序学习；框架接口以当前官方文档为准，最终项目继续使用主流框架完成。

### 第一阶段：现在重点学习

- 第一章：初识智能体——快速阅读，建立概念地图。
- 第三章：大语言模型基础——重点掌握API、Prompt和模型局限。
- 第四章：经典范式——重点学习并动手实现ReAct；理解Plan-and-Solve与Reflection。

### 第二阶段：项目开发时学习

- 第七章：从零构建Agent框架——用于理解Agent Loop内部实现，不作为最终项目主框架。
- 第八章：记忆与检索——对应产品资料知识库和RAG。
- 第九章：上下文工程——对应多轮需求与技术服务上下文。
- 第十章：通信协议——重点理解MCP。
- 第十二章：性能评估——用于建立项目测试集和评估报告。

### 第三阶段：选择性参考

- 第六章：只在后期学习LangGraph相关内容。
- 第十三、十四章：参考真实项目的架构和任务拆解方式，并改造成海洋设备场景。
- 第十六章：参考毕业设计、README和项目包装方法。

### 当前暂不投入主要时间

- 第二章发展史：感兴趣时选读。
- 第五章低代码平台：了解即可。
- 第十一章Agentic RL：属于进阶模型训练内容，当前求职项目不需要。
- 第十五章赛博小镇：案例有趣，但与当前企业项目关联较弱。

每次教材学习采用：阅读原理 → 运行代码 → 用自己的话解释 → 改造成海洋设备示例 → 记录问题。

## 五、核心概念笔记

### 1. 什么是 AI Agent

用自己的话总结：

> AI Agent 是一个能理解任务、判断下一步行动，并调用工具完成任务的 AI 程序。

Agent 的核心循环：

```text
接收任务
→ 判断下一步
→ 选择并调用工具
→ 查看工具结果
→ 判断是否继续
→ 输出最终答案
```

### 2. Agent 的组成

| 概念 | 大白话解释 | 海洋设备场景 |
|---|---|---|
| LLM | Agent 的大脑 | 理解客户需求并判断下一步 |
| Instructions | 岗位职责 | 规定它是海洋设备技术顾问 |
| Tools | Agent 的手 | 查询产品、设备档案和工单 |
| Knowledge/RAG | 企业资料库 | 检索产品说明书和技术文档 |
| Memory/Session | 工作笔记 | 记住客户之前提出的水深要求 |
| Workflow | 企业办事流程 | 需求分析、选型、确认和交付 |
| Guardrail | 权限与规章制度 | 禁止编造参数或擅自报价 |
| Tracing | Agent 的工作录像 | 查看它调用了什么工具以及原因 |

### 3. 当前理解

- Agent 不是一种新的大模型，而是围绕大模型建立的一套程序。
- 大模型负责理解和判断，工具负责查询数据或执行真实操作。
- 普通 Python 函数注册为 Tool 后，就可以成为 Agent 的业务能力。
- 真实企业 Agent 的重点不只是聊天，还包括工具、数据、权限、流程、测试和部署。

### 4. 从真实开源项目理解 Agent

#### OpenHands：软件开发 Agent

- 用户目标：修复 Bug、修改代码或完成开发任务。
- 可用工具：读取和修改文件、运行终端命令、使用浏览器、执行测试。
- 工作循环：查看代码 → 修改代码 → 运行测试 → 阅读错误 → 继续修改。
- 关键启发：Agent 不只有大模型，还需要安全的执行环境、错误反馈和明确的完成标准。

#### Browser Use：浏览器操作 Agent

- 用户目标：搜索网页、填写表单、提取内容或完成多步骤网页任务。
- 可用工具：打开网页、点击、输入、滚动、切换标签页、截图和提取内容。
- 工作循环：观察网页 → 选择动作 → 执行动作 → 获取新网页状态 → 继续判断。
- 关键启发：工具执行后的结果必须重新交给 Agent，Agent 才能根据环境变化决定下一步。

#### GPT Researcher：研究报告 Agent

- 用户目标：围绕某个问题完成资料调查并生成带来源的报告。
- 可用工具：搜索、抓取网页、摘要、来源追踪和报告生成。
- 工作流程：规划研究问题 → 搜集资料 → 汇总与筛选 → 生成报告。
- 关键启发：真实 Agent 经常是“固定工作流 + 局部自主判断”，并不是每一步都完全自由发挥。

#### 三个项目的共同结构

```text
目标
→ Agent 判断下一步
→ 调用工具操作外部环境
→ 获取真实反馈
→ 保存当前状态
→ 检查是否完成或是否需要人工介入
→ 输出结果
```

框架和成品项目的区别：

- OpenHands、Browser Use、GPT Researcher 是针对具体任务构建的 Agent 产品或应用。
- OpenAI Agents SDK、LangGraph、CrewAI 是帮助开发者构建 Agent 的框架。
- 框架相当于建筑工具，具体 Agent 项目才是最后建成的房子。

### 5. Agent、Skill 和 Tool 的区别

用企业员工来比喻：

- Agent 是负责接收目标、判断情况并推进任务的员工。
- Skill 是员工需要时读取的岗位技能手册或标准作业流程。
- Tool 是员工真正用来查询或执行操作的工具和业务系统。

Skill 可以包含说明、参考资料、模板以及可选脚本，但通常不会自己接收目标并持续运行。它需要由 Agent 选择和执行。一个 Agent 可以根据不同任务加载多个 Skill，一个 Skill 也可以指导 Agent 组合使用多个 Tool。

海洋设备场景示例：

```text
技术服务 Agent
├── Skill：CTD 通信故障诊断流程
│   ├── 检查供电
│   ├── 检查通信接口
│   └── 判断是否需要返厂
├── Tool：查询设备档案
├── Tool：分析设备日志
└── Tool：创建维修工单
```

关键区别：

- Agent 决定当前目标以及下一步做什么。
- Skill 告诉 Agent 某类任务应该怎样可靠地完成。
- Tool 负责读取真实数据或改变外部系统状态。
- Knowledge/RAG 提供事实资料，Skill 提供处理这些资料的方法和步骤。

## 六、每日学习记录

### Day 1 — 2026-08-18

**今日主题**

认识 AI Agent，以及普通聊天模型与 Agent 的区别。

**今天学会的内容**

- 理解了 Agent 的基本定义。
- 理解了 Agent Loop：判断、调用工具、观察结果、继续处理。
- 知道了 LLM、Tools、RAG、Memory、Workflow、Guardrail 和 Tracing 的大致作用。
- 明白了 Agent 开发本质上包含大量 Python、API 和业务系统集成工作。
- 确定以海洋设备智能售前与技术服务为企业项目场景。

**完成情况**

- [x] 了解 AI Agent 的基本概念
- [x] 了解 Agent 与普通聊天机器人的区别
- [x] 确定企业业务场景
- [x] 建立学习档案
- [ ] 编写第一个海洋设备查询工具
- [ ] 将查询工具连接到 OpenAI Agent

**当前疑问**

- 暂无，后续在实际编码过程中补充。

**下一步**

学习 Tool Calling，建立一个模拟海洋设备产品库，并编写第一个产品查询工具。

**补充学习：真实开源 Agent 项目**

- [x] 了解 OpenHands 如何使用文件、终端和测试完成软件开发任务
- [x] 了解 Browser Use 如何通过观察网页和执行动作完成浏览器任务
- [x] 了解 GPT Researcher 如何通过规划、搜索和汇总生成研究报告
- [x] 理解 Agent 框架与具体 Agent 产品的区别
- [x] 理解真实 Agent 通常是固定流程与自主判断的结合
- [x] 理解 Agent、Skill、Tool 和 Knowledge 的区别

### Day 2 — 2026-08-19

**今日主题**

理解 Tool Calling，并用纯 Python 实现第一个海洋设备查询工具。

**今日安排（约90分钟）**

1. 复习 Agent、Skill、Tool 的区别：10分钟。
2. 理解 Tool Calling 的完整数据流：15分钟。
3. 使用 Pydantic 定义海洋设备数据模型：20分钟。
4. 建立厂家公开产品库并实现 `search_products`：30分钟。
5. 编写测试、记录结果和提交 Git：15分钟。

**今日验收标准**

- [ ] 能用自己的话解释什么是 Tool Calling
- [x] 建立4条厂家公开海洋设备数据
- [x] 根据水深和测量参数筛选产品
- [x] 查询不到产品时返回明确结果，而不是编造型号
- [x] 为正常、无结果和边界条件编写测试

**今日暂不做**

- 不接入真实大模型API
- 不做多Agent
- 不做RAG和向量数据库

**产品数据处理方案**

- 第一版不依赖企业内部手册，使用 Sea-Bird Scientific、RBR 等厂家官网和公开规格书建立学习数据集。
- 厂家、型号、产品系列、用途、测量参数、水深等级、采样率、精度、供电、通信、壳体和扩展能力采用统一字段。
- 每条产品记录保存来源网址、资料版本或发布日期、采集日期和核验状态。
- 官网没有明确给出的价格、库存、交期、兼容性和服务承诺一律不推测。
- 公开厂家型号标记为“第三方公开产品”，不得写成海询自研、在售或代理产品。
- 为测试人工补充的数据必须标记为 `simulated`，不得与官方参数混合。
- 后续获得脱敏的企业目录或手册时，放入独立的“企业内部数据层”，经过核验后再用于真实选型。

**下一步**

完成纯 Python 工具后，将同一个函数注册给 OpenAI Agents SDK，对比“普通函数调用”和“Agent自主选择工具”的区别。

**实际完成结果**

- 使用 Pydantic 建立了产品、测量指标、深度配置、采样配置和资料来源模型。
- 录入了 Sea-Bird SBE 19plus V2、SBE 16plus V2、SBE 37 MicroCAT，以及 RBRconcerto³ C.T.D 共4条公开产品记录。
- 完成 `search_products`：支持水深、测量参数和部署方式筛选，并支持温度、盐度、压力等中文名称。
- 完成 `get_product_spec`：按型号或产品ID查询，找不到时返回 `None`。
- 使用 Python 标准库 `unittest` 编写并通过8个测试，无需额外安装 pytest。

**今天理解到的数据规则**

- “厂家没写”不等于“不支持”，但程序不能把未知当成满足条件。
- 压力等选配能力可以进入候选集，最终结果必须提醒用户需要相应配置。
- 盐度通常是由电导率、温度和压力计算出来的派生参数，不是传感器直接测量值。
- 同一型号可能存在不同壳体、深度、压力传感器和采样率，不能粗暴合并成一个无条件参数。

**代码位置**

- `ocean_agent/models.py`：统一数据模型。
- `ocean_agent/product_data.py`：厂家公开产品数据。
- `ocean_agent/tools.py`：查询工具。
- `tests/test_tools.py`：自动化测试。

### Day 3 — 2026-08-20

**今日主题**

为 Agent 项目建立独立 Python 环境，并安全管理 API Key。

**实际完成结果**

- 创建 Conda 环境 `ocean-agent`，使用 Python 3.12.13。
- 在独立环境中安装 Pydantic、pydantic-settings 和 python-dotenv。
- 在工程根目录创建 `.env`，只用于保存本机 API Key。
- 创建 `.env.example`，用于说明配置格式但不包含真实密钥。
- 创建 `ocean_agent/config.py`，集中加载和校验配置。
- 使用 `SecretStr` 避免配置对象和普通日志显示完整 Key。
- 新增3个配置测试；当前产品与配置共11个测试全部通过。

**今天理解到的配置规则**

- API Key 属于密钥，不能硬编码到 Python 文件，也不能提交到 GitHub。
- `.env` 保存个人本地配置，`.env.example` 只保存字段示例。
- 业务代码通过统一的 `get_settings()` 获取配置，不在各文件重复读取环境变量。
- Conda 环境隔离项目依赖，避免污染 `base` 环境。

**下一步**

为 API 账户补充可用额度后，运行第一次真实 Agent 回答并观察完整 Tool Calling 过程。

**Agents SDK 接入结果**

- 安装并固定 `openai-agents==0.22.0`。
- 创建 `agent_tools.py`，把确定性的产品筛选包装为 `search_ocean_products` Tool。
- 创建 `agent.py`，使用 `Agent + Runner` 完成命令行运行入口。
- 默认模型设置为适合低成本学习的 `gpt-5.6-luna`，可通过 `OPENAI_MODEL` 替换。
- Agent 指令要求先查产品目录，禁止编造型号，并提示选配、派生参数和配置风险。
- 离线测试扩展到16个，全部通过。
- 第一次真实请求已到达 OpenAI API，但账户返回 `credit_balance_exhausted`；需要充值后重试。

**今天理解到的 Agent Loop**

- `Agent` 保存角色、规则、模型和可用工具。
- `Runner` 负责循环：调用模型、执行 Tool、把 Tool 结果交还模型、取得最终回答。
- Tool 不是让模型直接运行任意代码，而是只开放我们明确注册的 Python 函数。
- 大模型负责理解自然语言和组织答案；产品是否满足条件仍由确定性代码判断。

**本地模型部署**

- 确认本机配置为 RTX 4090 24GB、125GB 内存，适合运行27B量化模型。
- 从 Qwen 官方仓库确认 `Qwen3.8-27B` 已发布，使用 Apache-2.0 许可。
- 部署 Unsloth 基于官方权重制作的 `UD-Q4_K_M` GGUF，模型与视觉投影合计约17GB。
- 系统 Ollama 0.12.3 不支持新模型架构，因此在工作盘隔离安装 Ollama 0.32.14。
- 保留原服务 `127.0.0.1:11434`，新增 Qwen3.8 服务 `127.0.0.1:11435`。
- 新服务已设置为用户级 systemd 自动启动，不需要 sudo，也不影响原有模型。
- 中文生成测试通过：8K上下文、100% GPU，首次加载约21.7秒，生成约13.6 token/s。
- Tool Calling 测试通过：能把“5000米温盐深”正确转换为产品查询函数参数。

**本地模型下一步**

已完成：海洋设备 Agent 默认使用本地 Qwen3.8，并保留切换 OpenAI API 的配置开关。

**本地 Qwen 接入 Agents SDK**

- 新增 `MODEL_PROVIDER` 配置，默认值为 `ollama`；不需要 OpenAI Key 也能运行。
- 使用 Ollama 的 OpenAI 兼容接口 `http://127.0.0.1:11435/v1`。
- 使用 Agents SDK 的 `OpenAIChatCompletionsModel` 适配本地模型。
- Agent、Runner、产品 Tool 和业务指令保持不变，只替换模型适配层。
- 本地模式关闭 OpenAI Tracing，避免尝试向 OpenAI 上传运行轨迹。
- 添加1400 token的单轮输出上限，避免本地推理无限拉长。
- 配置和 Agent 测试扩展到17个，全部通过。
- 完整 Agent Loop 测试通过：Qwen 能从“5000米温盐深”需求中提取参数、调用产品 Tool，并基于 Tool 结果返回3个候选及配置风险。

**今天理解到的模型与 Agent 的关系**

- Agent 是工作流程：理解任务、决定是否调用工具、执行工具、根据结果继续回答。
- 模型是 Agent 的判断引擎，可以替换，但不同模型的 Tool Calling 能力和协议兼容性不同。
- 框架支持某个模型，不等于模型效果完全相同；实际项目仍需用测试案例评估准确率、速度和稳定性。

**第二个 Tool：查询具体产品详情**

- 将已有的确定性函数 `get_product_spec()` 包装为 SDK Tool：`get_ocean_product_details`。
- 用户提出选型条件时，模型应选择 `search_ocean_products`。
- 用户询问明确型号的参数时，模型应选择 `get_ocean_product_details`。
- 未收录型号返回 `found=false`，并明确要求模型不得根据记忆补全参数。
- 自动化测试从17个增加到20个，全部通过。
- 使用本地 Qwen 进行真实路由测试，模型正确选择 `get_ocean_product_details`，没有误用产品筛选 Tool。
- 最终回答正确引用了 SBE 19plus V2 SeaCAT 的壳体深度、RS-232、采样率及配置风险。

**这一小步理解到的重点**

- Python 函数负责可靠地查数据。
- Tool Schema 告诉模型函数叫什么、什么时候用、需要什么参数。
- 模型负责根据用户意图选择 Tool，而不是自己执行 Python。
- Runner 负责执行 Tool，并把结果交回模型继续回答。

**官方文档学习：Agent definitions**

- `Agent` 是 Agents SDK 工作流的核心配置单元。
- `name` 是 Agent 的可读名称，主要用于识别和运行记录。
- `instructions` 定义岗位、约束和回答风格。
- `model` 是负责理解与判断的模型；本项目使用本地 Qwen3.8。
- `model_settings` 调整模型行为；本项目限制单轮最多生成2000 token。
- `tools` 是 Agent 可以直接选择和调用的能力。
- 创建 `Agent(...)` 只是完成配置，真正开始工作需要交给 `Runner`。
- 当前先保持一个职责明确的海洋设备 Agent，不急于增加多 Agent。

### Day 4 — 2026-08-21

**今日主题：观察 Runner Loop**

- 理解模型决定“调用 Tool 或给最终答案”，Runner 负责执行、循环和终止。
- 当前 SDK 提供 `run()`、`run_sync()` 和 `run_streamed()`，没有 `run_unsync()`。
- 本地 SDK 默认 `max_turns=10`，用于防止一次 Run 无限调用模型。
- 为命令行增加 `--debug`，显示模型调用轮数、Tool 名称、参数和返回状态。
- `run_agent_result()` 保留完整 `RunResult`；原有 `run_agent()` 仍只返回最终文本。
- 调试输出不展示完整产品 JSON，避免信息过多，同时保留观察 Loop 所需证据。
- 将统计式调试输出升级为逐轮时间线：模型请求 Tool、Runner 执行、下一轮生成最终答案、Runner 结束 Loop。
- 时间线读取 `raw_responses` 和 `new_items`，但不展示模型的隐藏推理内容。
- 第一次多 Tool 实验中，本地模型把 JSON `null` 传成字符串 `"null"`；Tool 报错后 Agent 自行改查三种部署方式并继续完成比较，证明 Runner 可以把 Tool 错误交回模型恢复。
- 为兼容本地模型，将 `"null"`、`"none"` 和空字符串统一视为未指定部署方式。
- 多产品比较回答超过原1400 token上限，因此调整为2000，避免最终答案表格被截断。

**第三个 Tool：产品比较**

- 新增普通 Python 函数 `compare_ocean_products_data(product_ids)`，只负责对齐已核验数据，不生成主观推荐。
- 新增 SDK Tool `compare_ocean_products`，输入使用搜索 Tool 返回的产品 ID。
- 比较字段包括深度、部署方式、参数角色、采样配置、通信接口、壳体和配置风险。
- 找不到的产品 ID 会进入 `missing_product_ids`；少于两个有效产品时禁止比较并要求不得编造。
- 第一次正常多 Tool 时间线为3轮模型、2次 Tool：搜索 → 比较 → 最终答案。
- 时间线同时发现模型在用户未指定时自行假定 `profiling`；新增指令要求未指定部署方式必须传 `null`，不得自行缩小候选范围。
- 最终真实验证通过：第1轮搜索保留3个候选，第2轮使用3个真实 `product_id` 比较，第3轮生成完整答案并正常结束；共3轮模型、2次 Tool。

### Day 5 — 2026-08-21

**今日主题：多轮会话与状态管理**

- 一次 `Runner.run_sync()` 是一轮用户对话；Runner 内部的 Tool Loop 结束，不代表整个聊天结束。
- 使用 SDK 的 `SQLiteSession` 保存用户消息、模型回复和 Tool 调用记录。
- 相同 `session_id` 会读取同一段历史，不同 ID 的聊天互相隔离。
- 新增 `ocean_agent/sessions.py`，集中负责创建会话数据库和校验会话 ID。
- `run_agent_result()` 和 `run_agent()` 新增可选 `session` 参数，核心变化是把它传给 `Runner.run_sync()`。
- CLI 新增 `--session-id` 和 `--chat`，支持连续对话以及程序重启后的历史恢复。
- 会话数据库存放在 `.agent_data/conversations.db`，并通过 `.gitignore` 防止用户聊天记录进入 Git。
- 全部自动化测试增加到28个并通过；Session 测试确认同 ID 恢复历史、不同 ID 相互隔离。
- 本地 Qwen 真实两轮验证通过：第一轮搜索得到3个候选；第二轮只说“比较它们”，模型从会话历史取得上一轮的3个 `product_id`，直接调用比较 Tool。
- 测试过程中发现 `SQLiteSession.add_items()` 和 `get_items()` 是异步方法；Runner 内部会负责等待，直接测试时必须使用 `await`。

### Day 6 — 2026-08-23

**今日主题：第一版本地 RAG 与技术资料 Tool**

- 新增 `TechnicalDocumentChunk`，把技术资料保存成可独立检索和引用的短片段。
- 新增 `document_data.py`，整理7个 Sea-Bird/RBR 技术资料片段，每段都保留厂家来源。
- 当前内容是厂家产品页和数据表的中文整理，不冒充完整说明书，也不代表海询公司的服务承诺。
- 新增普通 Python 函数 `search_documents()`，支持型号过滤、中文问题和相关度排序。
- 中文检索会提取二元片段，例如“通信接口”可拆出“通信”“信接”“接口”，不要求用户手工加空格。
- 新增 SDK Tool `search_ocean_documents`，返回资料内容、章节、来源 URL 和相关度分数。
- Agent 新增规则：操作、接线、维护、校准和故障问题必须先查技术资料；没有结果时不得凭记忆补写。
- 第一次测试发现“故障码维修步骤”误命中了含“接线步骤”的片段；将最低相关度提高后消除误命中。
- 真实 Qwen 测试中，模型把完整型号缩写为 `SBE 19plus V2`；产品查询因此增加“仅唯一命中才接受”的安全简称匹配。
- 修复后真实 Runner 时间线为2轮模型、1次 `search_ocean_documents`：检索资料 → 基于片段回答并结束。
- 自动化测试从28个增加到37个并全部通过。

**今天理解到的 RAG**

- RAG 的核心不是让模型记住文档，而是回答前先检索相关片段。
- Session 保存“刚才聊了什么”，RAG 检索“企业资料里写了什么”，两者职责不同。
- 文档必须切成适合独立理解的片段，并为每段保存来源。
- 检索到不相关内容可能比完全没搜到更危险，因此必须测试误命中。
- 当前是关键词检索，下一版会学习 Embedding 和向量相似度，但 Agent Tool 接口可以保持不变。

### Day 7 — 2026-08-24

**今日主题：本地 Embedding 与余弦相似度实验**

- 确认本机 `nomic-embed-text:latest` 为137M参数的 Embedding 模型，输出768维向量。
- 新增 `embedding_search.py`，通过 Ollama `/api/embed` 调用本地模型。
- 文档输入自动增加 `search_document:` 前缀，用户问题增加 `search_query:` 前缀。
- 新增 `create_embedding()` 和 `create_embeddings()`，支持单条和批量文本编码。
- 使用 Python 标准库实现 `cosine_similarity()`，没有引入 NumPy 等额外依赖。
- 新增 `semantic_search()`，按余弦相似度从高到低返回文本。
- 新增自动化测试，覆盖相同方向、垂直向量、维度错误、Ollama请求格式和搜索排序。
- 新增真实测试脚本 `tests/embedding_similarity_demo.py`，可以打印中英文句子的实际相似度。
- 全部自动化测试从37个增加到43个并通过。

**真实相似度实验结论**

- 中文问题“如何把海洋仪器连接到计算机？”没有正确匹配 RS-232 资料，天气句子反而排第一。
- 相同含义的英文对照组正确把 RS-232 通信排第一，把杭州天气排最后。
- 这说明余弦计算和调用链路能够工作，但当前 Nomic 模型的中文语义能力不适合本项目。
- 本次没有把 Embedding 接入正式 RAG，避免降低现有中文检索准确率。
- 下一步应选择并测试中文或多语言 Embedding 模型，再决定是否建立混合检索。

**多语言 Embedding 横向评测**

- 新建独立 Conda 环境 `embedding-benchmark`，避免把 PyTorch 和模型依赖装进正式 Agent 环境。
- 新增 `benchmarks/ocean_embedding_cases.json`：16 个中英资料片段、10 个有答案问题和2个无答案问题。
- 测试覆盖中文查英文、英文查中文、中查中、英查英，并使用同产品不同参数作为较难的错误候选。
- 新增 `benchmarks/run_embedding_benchmark.py`，统一计算 Top-1、Recall@3、MRR、正确答案领先分差和无答案最高分。
- 在 RTX 4090 上依次实测 Qwen3-Embedding-0.6B、BGE-M3、multilingual-e5-large-instruct 和 Nomic Embed Text v2 MoE。
- 四个模型的10个有答案案例全部 Top-1 命中；平均领先分差依次为 BGE-M3 0.1406、Qwen3 0.1293、Nomic v2 0.1195、E5 0.0585。
- 中文问题检索英文资料时，Qwen3 的平均领先分差最高，为0.1602；BGE-M3 的总体表现最均衡，作为下一阶段首选。
- Nomic v2 需要额外安装 `einops` 并允许加载模型仓库代码，部署复杂度略高于前三者。
- 新增 `benchmarks/RESULTS.md`，记录本机结果、公开基准、选择理由和测试局限。
- 当前只有10个有答案案例，尚不足以直接改造正式 RAG；下一步先把领域测试扩展到至少30～50条。

**40题困难压力测试**

- 新增 `ocean_embedding_stress_cases.json`，继承基础集后共包含28个资料片段、32个有答案问题和8个无答案问题。
- 新增口语改写、中英混写、型号缩写、错别字、同产品不同参数、同参数不同产品和多正确答案测试。
- 修正指标定义：将“前三至少命中一个”命名为 Hit@3，并单独计算多答案 Recall@3 与 nDCG@3。
- 增加重复ID、无效候选和正确资料不在候选列表等数据结构校验，并新增3项自动化测试。
- 困难集 Top-1：Qwen3 96.9%、E5 93.8%、Nomic v2 93.8%、BGE-M3 87.5%；四者 Hit@3 均为100%。
- 上一轮基础测试推荐 BGE-M3，但困难集证明该结论不够稳定；当前改为优先选择 Qwen3-Embedding-0.6B。
- Qwen3 唯一错误是把 MicroCAT 的“压力选配”问题匹配到 SBE 16plus 的压力选配资料，说明型号约束仍然必要。
- 8个无答案问题证明所有模型的有答案/无答案分数都重叠，不能只靠一个余弦相似度阈值决定拒答。
- 下一步检索架构应是型号/关键词约束、向量召回Top-3、重排或证据判断，而不是只取向量Top-1。

**切换到Qwen3 Embedding**

- 通过Ollama部署 `qwen3-embedding:0.6b`，模型文件约639 MB，输出1024维向量。
- 本机同时运行11434和11435两个Ollama服务；11435进程没有共享模型目录写权限，因此改用具备权限的11434服务下载，下载后11435可以正常读取同一模型。
- 将项目默认 `OLLAMA_EMBEDDING_MODEL` 从 `nomic-embed-text:latest` 改为 `qwen3-embedding:0.6b`。
- Qwen查询输入使用 `Instruct: ... Query:` 任务说明，文档输入保持原文；Nomic配置仍保留原有 `search_query:` 与 `search_document:` 兼容逻辑。
- 新增Qwen查询格式、Qwen文档格式和Nomic兼容测试，项目自动化测试从43项增加到45项并全部通过。
- 真实Ollama测试中，中文和英文连接问题都把RS-232资料排第一、天气资料排最后，向量维度为1024。
- 本次完成的是模型部署、默认配置和独立语义搜索切换，正式 `search_ocean_documents` 仍使用关键词检索。

## 七、问题与错误记录

遇到问题时按照下面格式追加，不要只记录错误信息，还要记录最后是怎样解决的。

### 问题模板

- 日期：
- 问题现象：
- 原因：
- 解决方法：
- 我从中学到了什么：

## 八、项目里程碑

- [x] 里程碑1：单 Agent 能调用至少3个工具
- [x] 里程碑2：支持多轮会话和运行追踪
- [x] 里程碑3：建立海洋设备技术资料知识库（第一版本地关键词检索）
- [ ] 里程碑4：实现多 Agent 路由和人工审批
- [ ] 里程碑5：提供 FastAPI 和数据库持久化
- [ ] 里程碑6：建立至少30条自动评估案例
- [ ] 里程碑7：使用 Docker 启动完整项目
- [ ] 里程碑8：完成 GitHub README、架构图和演示视频
- [ ] 里程碑9：整理简历描述和面试问答

## 九、求职材料清单

- [ ] 完整项目代码
- [ ] 清晰的 README
- [ ] 系统架构图
- [ ] 本地运行说明
- [ ] Docker 部署说明
- [ ] API 使用示例
- [ ] 测试与评估报告
- [ ] 安全设计说明
- [ ] 2～3分钟项目演示视频
- [ ] 简历项目描述
- [ ] 面试问题与回答

## 十、每周复盘模板

每周结束时填写：

- 本周完成了什么：
- 最重要的三个知识点：
- 遇到的主要问题：
- 哪些内容仍然不清楚：
- 下周要完成什么：
- 本周实际学习天数：
- 本周 Git 提交次数：
